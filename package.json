{
  "name": "root",
  "private": true,
  "workspaces": [
    "plugins/*",
    "docs",
    "examples"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish --no-private -y from-package",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
