{
  "name": "root",
  "private": true,
  "workspaces": [
    "plugins/*"
  ],
  "scripts":{
    "build": "lerna run build",
    "publish": "lerna publish --no-private from-package",
    "test": "lerna run test"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
